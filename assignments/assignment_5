########### Assignment 5 ###########

# Now we'll talk a little bit more about functions

# As a reminder, here are some examples:
  # print() - returns whatever you enter in the parentheses in your output
  # len() - returns the length of whatever data structure you pass through it
  # range(startNum, endNum, skipNum) - returns a range of numbers; you can convert it to a list to see the full list of numbers
# Some data structure conversion functions:
  # str() - converts whatever you pass through it to a string
  # int() - converts whatever you pass through it to an integer, typically do this with floats or numbered strings ('5')
  # list() - converts whatever you pass through it to a list, can use this on range()

# There are plenty more (especially in libraries) but we can also create our own functions.
# We want to create our own functions when we have repetitive chunks of code that can be reused.
# Let's look at how you would create a function:

def my_function(input):

  input += 1
  print(f'your input is now {input}')

  return input

# The function basically adds one to your input and prints the new value. The last line "return input" will return 
# the new value of input. You can assign this to a new variable. See the function in action below:

variable = my_function(1)

# "variable" will now be assigned to the value of 2 and "your input is now 2" will be printed

########################################################################################################################################

# 1. Going back to the last assignment, let's load the 'attention' dataset again. Also load the 'fmri' dataset.
# Create a function that returns the 'subject' column from either dataset.

# 2. Import numpy and find the square root function - you can either look online or use the tab key to find it.
# Also use this new exponent operator ** (ex. 2 ** 2 = 4).
# Use this to create a standard deviation function and recreate the mean function we made together. Return the SD and mean, respectively.
# Just as a reminder, this is the standard deviation formula: square_root(sum(every_value - mean) ** 2 / (num_of_values - 1))
# 2b. Using our standard deviation and mean functions, take the standard deviation and mean of the 'signal' column in the fmri dataset.
# Output here should be: mean = 0.003539768564096969; SD = 0.09388556308642104

# 3. Copy and paste these lists and assign them to variables: 

["Mateo V.", "Freya H.", "Julian C.", "Isla M.", "Silas T.", "Eliana B.", "Kai G.", "Nora F.", "Archer H.", "Lyra B."]
["Jasper K.", "Leo M.", "Julian C.", "Sienna P.", "Ethan W.", "Chloe D.", "Oliver J.", "Maya R.", "Mateo V.", "Freya H."]

# Let's assume these are participant names in different studies. Sometimes we don't want pts to be in two studies at once so let's find
# which ones are in both studies.
# Create a function that will take in two lists and checks if there are any names that overlap. Return the number of overlapping pts.
# Output of function should be 3.
